import styles from '../styles/Logo.module.scss';

export default function Logo(){
    return (
        <section className={styles.logoWrapper}>
            <p>m<sub>y</sub>home</p>
        </section>
    )
}